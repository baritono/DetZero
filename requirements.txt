# Most numeric dependencies (numpy in particular) are hard-pinned by
# waymo-open-dataset-tf-2-12-0, so we avoid fighting its resolver here.
# See docs/INSTALL.md for the full Ubuntu 24.04 compatibility matrix.
numba>=0.59,<0.60
tensorboardX
easydict
pyyaml
tqdm
opencv-python
tabulate
kornia
scipy
addict
